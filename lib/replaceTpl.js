function replaceHtml(htmlStr, data) {
    return htmlStr.replace(/<!--title-->/, function() {
        return data.title ? `<title>${data.title}</title>` : `<title>网易菠萝</title>`;
    })
            .replace()
            .replace()
            .replace()
            .replace()
            .replace()

}